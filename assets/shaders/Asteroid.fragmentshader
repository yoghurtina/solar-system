#version 330 core
out vec4 FragColor;

in vec2 TexCoords;

uniform sampler2D asteroid_texture;

void main()
{
    FragColor = texture(asteroid_texture, TexCoords);
    FragColor = vec4(1.0, 0.0, 0.0, 1.0);
}
